;; yasnippet
(define-key yas-minor-mode-map (kbd "<C-tab>") 'yas-expand)


;; help mode
;; I-Search 不知道為什麼在 help-mode 會失敗
;; 但 serach 好難用
;; (define-key help-mode-map (kbd "C-s") 'search-forward)
;; (define-key help-mode-map (kbd "C-r") 'search-backward)
;; (define-key help-mode-map (kbd "C-M-s") 'search-forward-regexp)
;; (define-key help-mode-map (kbd "C-M-r") 'search-backward-regexp)
